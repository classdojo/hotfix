// Generated by CoffeeScript 1.4.0
(function() {
  var MAX_PAGE_REFRESH_INTERVAL, domready, refreshPage, showMessage, template;

  domready = require("domready");

  template = require("./template");

  MAX_PAGE_REFRESH_INTERVAL = 1000;

  domready(function() {
    PUBNUB.subscribe({
      channel: "hotfix_refresh",
      callback: function(payload) {
        return setTimeout(refreshPage, Math.random() * MAX_PAGE_REFRESH_INTERVAL, payload);
      }
    });
    return showMessage({
      text: "hello world!",
      critical: true
    });
  });

  /*
   refreshes the page
  */


  refreshPage = function(payload) {
    return console.log(JSON.stringify(payload));
  };

  showMessage = function(data) {};

}).call(this);
